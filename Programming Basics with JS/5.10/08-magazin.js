function totalSum(dogCount, catCount) 
{
    let count1 = dogCount * 2.5;
    let count2= catCount * 4;
    let countTotal = count1 + count2;
    console.log(`${countTotal} lv.`);
}

totalSum(5,4);
totalSum(13,9);